import styled from "styled-components";

const Heading1 = styled.h1`
  margin-bottom: 1em;

  font-size: 1em;
  font-weight: bold;

  &:not(:first-of-type) {
    margin-top: 1.5em;
  }
`;

export default Heading1;
